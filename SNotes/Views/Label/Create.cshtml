
@using System.Web.Mvc.Html


<div class="row">
    <div class="col-md-3">

        @{Html.RenderAction("LabelList", "Label"); }

    </div>

    <div class="col-md-8">
        <div style="width: 85%">
            @{
                Html.RenderPartial("_createLabelPartial");
            }
        </div>
    </div>

    <div class="col-md-1">
    </div>
</div>
















































@*@model SNotes.ViewModels.AddLabelViewModel

@{
    ViewBag.Title = "Create";
}

<h2>New Label</h2>
 
    @using(Html.BeginForm("Create", "Label"))
    { 
        <div class="form-group">
            @Html.LabelFor(m => m.Name)
            @Html.TextBoxFor(m => m.Name, new {@class = "form-control"})
        
        </div>
    
        <button type="submit" class="btn btn-default">Submit</button>
    }*@

