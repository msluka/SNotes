
@using System.Web.Mvc.Html


<div class="row">
    <div class="col-md-3">

        @{Html.RenderAction("LabelList", "Label"); }

    </div>

    <div class="col-md-8">
        <div style="width: 85%">
            @{
                Html.RenderPartial("_addLabelToNotePartial");
            }
        </div>
    </div>

    <div class="col-md-1">
    </div>
</div>







@*@model SNotes.ViewModels.AddLabelToNoteViewModel

@{
    ViewBag.Title = "Add Label To Note";
}

<h2>New Label</h2>
 
@using(Html.BeginForm("AddLabelToNote", "Note"))
{ 
    <div class="form-group">
        
        @Html.TextBoxFor(m => m.LabelName, new {@class = "form-control"})
        
    </div>
    @Html.HiddenFor(m => m.NoteId)
    <button type="submit" class="btn btn-default">Submit</button>
}*@

